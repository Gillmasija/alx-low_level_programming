#include "main.h"

/**
 *_strcat - Entry point
to append
 *
 *Return: Apointer to the description
 */
char *_strcat(char *dest, char *src);
{
unsigned int i;
for (i = 0; i < n; i++)
s[i] = b;
return (s);
}
